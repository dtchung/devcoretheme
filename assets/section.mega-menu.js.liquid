$(document).on('ready', function() { 
   
  	 /* $.loadScript = function (url, callback) {
        $.ajax({
            url: url,
            dataType: 'script',
            success: callback,
            async: true
        });
      }     
      if (typeof someObject == 'undefined') $.loadScript("{{ 'slick.min.js' | asset_url }}", function(){
          //Stuff to do after someScript has loaded     
          
      })  	
      
      $('head').append('<link rel="stylesheet" href="{{ 'slick.css' | asset_url }}" type="text/css" />');
      $('head').append('<link rel="stylesheet" href="{{ 'slick-theme.css' | asset_url }}" type="text/css" />');                 
      
      */
  		
});